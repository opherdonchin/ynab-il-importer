# Active Plan

## Workstream

Family `2026_06_06` live Bank Leumi reconciliation is blocked by a live cleared-balance drift. Resolve the YNAB/source alignment before resuming Pilates, then Aikido.

## Current State

- `family / 2026_06_06` was uploaded and artifact-closeout reports were previously clean after a source-boundary mapping fix:
  - raw sources present: bank + MAX card
  - normalized sources: bank `139` rows, MAX card `194` rows
  - source-windowed YNAB snapshot: `1,478` rows for `2025-11-25` through `2026-06-18`
  - matched pairs: `120` rows
  - proposal artifact: `113` rows
  - reviewed artifact: `113` rows, `113` reviewed, `112` upload decisions
  - reviewed decisions: `create_target=111`, `update_target=1`, `ignore_row=1`
  - upload artifact: `create=111 | update=1`
  - upload was executed by the user; follow-up bank sync stamped `4` source rows onto `3` live YNAB transactions
  - the duplicate `2026-05-19` `7.00` ILS `30 ב-סברס בש בניין` Bank Leumi row was resolved by creating missing live YNAB transaction `de8714f3-b83e-41ef-930d-02769c2055ba` as `Campus Food / Opher food`
  - bank reconciliation executed and patched `64` live YNAB transactions; refreshed closeout reports are clean
  - prior final status was artifacts `present=13`, reports `clean=9`, live checks `clean=10`, but that status is now known to have missed live cleared-balance drift
  - the `2026-05-24` Bank Leumi `+3500` transfer from account `22523701` now fingerprints to `pilates leumi` and proposes `Transfer : Pilates Leumi / Pilates`
  - saved review work was merged onto the rebuilt proposal; pre-merge backup is `data/paired/2026_06_06/family_proposed_transactions_reviewed.backup_2026-06-06_173909.parquet`
  - the `2026-05-24` Pilates transfer is linked as `update_target` to existing YNAB transaction `4240dc88-3a16-4c17-96c6-b8c014587e39`; pre-link backup is `data/paired/2026_06_06/family_proposed_transactions_reviewed.backup_2026-06-06_234229.parquet`
- Live Bank Leumi verification now blocks correctly:
  - saved raw/derived Family Bank Leumi source ends at `2026-06-04` with final statement balance `10,654.99`
  - live YNAB Bank Leumi cleared balance is `17,592.70`, with no post-statement cleared YNAB transactions after `2026-06-04`
  - `context-run-status family 2026_06_06 --verify-live` reports `live blocked=1` with reason `YNAB cleared 17592.70 vs statement final 10654.99 + post-statement cleared 0.00 = 10654.99`
  - reconciliation code now treats live cleared balance as part of the source-target proof and rejects duplicate `BANK:V1` lineage across import-id and memo-marker channels
- Pilates and Aikido artifacts for `2026_06_06` were generated during initial processing, but should wait until the Family Bank Leumi drift is repaired or explicitly accepted.

## Recently Completed

- normalized the June previous Leumi card statement for Pilates account `x0602`
- normalized family direct bank/card sources for `2026_06_06`
- downloaded the family source-windowed YNAB snapshot
- normalized Pilates direct sources and family-category source rows
- normalized Aikido family-category source rows
- downloaded Pilates and Aikido source-windowed YNAB snapshots
- built family, Pilates, and Aikido review proposals
- fixed Leumi transfer fingerprint matching so numeric counterparty account identifiers can drive fingerprint-map rules before long digit runs are stripped from the final fingerprint
- rebuilt the Family normalized artifacts and proposal after the transfer mapping fix
- merged the saved Family reviewed artifact onto the rebuilt proposal and carried the accepted state onto the corrected `22523701` Pilates transfer row
- launched a fresh Family review app at `http://localhost:8504`
- converted the corrected `+3500` Pilates transfer from `create_target` to `update_target` against the existing YNAB row, then ignored the duplicate target-only review row
- regenerated Family upload artifacts; dry-run preflight is clean with `111` creates, `1` update, no duplicate keys, no existing import-id hits, and no manual-match candidates
- user executed the clean Family upload artifact
- applied Family bank match sync; `4` stamp updates were planned/executed and `3` live YNAB transactions were patched
- generated all Family bank/card closeout reports and isolated the Bank Leumi duplicate `7.00` ILS Campus Food/Sabres row
- created the missing duplicate `7.00` ILS Campus Food/Sabres YNAB transaction `de8714f3-b83e-41ef-930d-02769c2055ba`
- reran Family bank sync and reconciliation; Bank Leumi matched `139/139`, reconciliation patched `64` transactions, and the refreshed report has `0` updates planned
- previous `context-run-status family 2026_06_06 --verify-live` appeared clean before the live cleared-balance proof was added
- fixed reconciliation correctness checks so live cleared balance must equal statement final balance plus later cleared live transactions
- fixed lineage resolution so one bank row linked by import id and another live row linked by memo marker for the same `BANK:V1` is blocked as duplicate lineage
- verified the new live check blocks Family Bank Leumi with the current `6,937.71` ILS drift

## Next Steps

1. Review and repair the Family Bank Leumi live drift:
   - identify the live YNAB transactions causing the `17,592.70` vs `10,654.99` balance gap
   - prefer fixing duplicate or wrongly linked YNAB rows over manually adjusting the bank balance
   - rerun `context-run-status family 2026_06_06 --verify-live` and require live bank reconcile to be clean
2. Resume `pilates / 2026_06_06` from the repaired Family boundary:
   - verify whether existing Pilates artifacts need rebuilding because Family live YNAB changed during closeout
   - review Pilates before upload/reconciliation
3. Then process `aikido / 2026_06_06` from the repaired Family boundary:
   - verify whether existing Aikido artifacts need rebuilding because Family live YNAB changed during closeout
   - review Aikido before upload/reconciliation

## Working Rules

- Keep review decisions explicit in the review artifact; do not let unresolved accounting rows appear reviewed.
- Fix lineage or matching drift at the source/proposal boundary rather than by manually relinking live YNAB rows.
- Rebase saved reviewed artifacts onto the freshly rebuilt proposal before resuming review.
- Keep source scope, review scope, and closeout scope separate when they are semantically different.
